{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the iHub window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
