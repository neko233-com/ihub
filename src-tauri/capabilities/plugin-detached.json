{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "plugin-detached",
  "description": "Minimal local capability for host-owned detached plugin windows. The remote loopback iframe receives none of these permissions.",
  "local": true,
  "windows": ["plugin-detached-*"],
  "permissions": [
    "detached-plugin-host-commands",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
